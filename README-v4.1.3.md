# Addie Enterprises HTML v4.1.3 — iPad Layout Fix

## Fixes
- Removes overlapping fields on iPad
- Uses two columns on tablets
- Uses one column on phones
- Prevents date and number fields from overflowing
- Adds cleaner vertical and horizontal spacing
- Improves form button layout
- Makes profit preview responsive

## Upload
1. Upload every file in this ZIP to the root of the GitHub repository.
2. Replace matching files.
3. Commit with:
   `v4.1.3 - Fix iPad form layout`
4. Wait 2–3 minutes for GitHub Pages.
5. Refresh the Add/Edit Item page.
